% Figure 25.9 of [elma]
parent(bert, alice).
parent(bert, george).
parent(alice, derek).
parent(alice, pat).
parent(derek, frank).

ancestor(A,B) :- parent(A, B).
ancestor(A,B) :- parent(A, C), ancestor(C, B).

ancestor(A,B)?